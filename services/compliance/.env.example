# Owner: Compliance/OPA lane (see TEAM_OWNERSHIP.md)
# Only vars this service reads. Values reference root .env at compose level.

# Base of OPA's data API. app/opa_client.py appends /compliance/<framework>.
OPA_URL=http://opa:8181/v1/data
OPA_TIMEOUT_SECONDS=10

# Framework used when a job/request doesn't name one. CIS | NIST | STIG.
DEFAULT_FRAMEWORK=CIS

# Where the opa container mounts services/compliance/policies (see
# docker-compose.yml). Read by humans and the healthcheck, not by app code —
# this service never touches the .rego files itself, it only queries OPA.
POLICY_BUNDLE_PATH=/policies

POSTGRES_DSN=postgresql+asyncpg://netaudit:changeme_in_local_env@postgres:5432/netaudit

# Broker for the `compliance.evaluate_baseline` task from the Parsing lane.
CELERY_BROKER_URL=redis://redis:6379/0

# GraphRAG topology graph (app/graph_client.py). Optional enrichment, same
# resilience pattern as Parsing's RAG/vendor-fingerprint providers — leaving
# NEO4J_URI unset makes app/graph_client.build_topology_graph_provider()
# return a no-op provider, and evaluation proceeds with blast_radius: [].
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme_in_local_env
# Max hops the blast-radius traversal walks out from a device with findings.
GRAPH_BLAST_RADIUS_MAX_HOPS=3

# Unsupervised semantic anomaly detection (app/anomaly_client.py). Optional
# enrichment, same pattern as NEO4J_URI above — leaving LEARNING_URL unset
# makes build_anomaly_detection_provider() return a no-op provider, and
# evaluation proceeds with anomaly: {"status": "unavailable"}.
LEARNING_URL=http://learning:8003
