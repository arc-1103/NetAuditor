fastapi==0.115.0
uvicorn[standard]==0.32.0
celery==5.4.0
redis==5.1.1
httpx==0.27.2
pydantic==2.9.2
# [asyncio] pulls greenlet, which SQLAlchemy's async engine needs at runtime.
# Plain `sqlalchemy` only declares greenlet for x86_64/aarch64, so on an Apple
# Silicon dev machine every async DB call raises "the greenlet library is
# required" — Linux/Docker happens to get it anyway, which is what hides it.
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
# GraphRAG topology client (app/graph_client.py) — async driver for the
# blast-radius traversal over routing adjacency.
neo4j==5.24.0
