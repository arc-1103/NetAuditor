! CIS-FORTIOS-1.3.1 - Replace weak IPsec Phase 1 encryption
config vpn ipsec phase1-interface
    edit "{{ vpn_name | default('vpn1') }}"
        set proposal aes256-sha256
        set dhgrp 14
    next
end
