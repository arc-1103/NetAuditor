! CIS-FORTIOS-1.1.1 - Restrict administrative access to trusted hosts
! REVIEW: confirm {{ management_network | default('10.0.0.0') }} {{ management_netmask | default('255.255.255.0') }} before deployment
config system admin
    edit "{{ admin_user | default('admin') }}"
        set trusthost1 {{ management_network | default('10.0.0.0') }} {{ management_netmask | default('255.255.255.0') }}
    next
end
