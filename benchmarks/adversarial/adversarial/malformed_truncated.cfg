! NetAudit Engine adversarial-benchmark fixture — MALFORMED class
! Truncated mid-directive and containing a binary/control-character
! fragment, simulating a corrupted upload (partial write, wrong encoding,
! or a non-config file renamed to look like one) rather than a real device
! config with unusual-but-valid syntax.
version 17.9
hostname branch-core-trunc
service password-encryption
!
interface GigabitEthernet0/0
 description WAN uplink
 ip address 10.91.0
